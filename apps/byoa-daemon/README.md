# byoa-daemon

Go BYOA daemon(#63 起填充):配对/心跳/wake-stream 消费/run 生命周期,引擎适配器
(claude/codex/zcode/grok/cursor),单二进制,GitHub Releases 分发(ADR 0004)。
现役 TS daemon 源码在 `server/src/agents/computer/` + `agent-cli/`(npm 壳),切换日退役。
